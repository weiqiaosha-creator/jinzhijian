# 更新日志 (Changelog)

本文件记录「瑾之笺」各版本的功能变更。
版本留档规则：每个稳定版都会归档到 `releases/<版本>/`（完整可加载目录），
同时打 git tag `v<版本>`。最新版始终位于 `stable/`，旧版本不会覆盖、可随时回退使用。

## [v2.0.21] - 2026-09-01
### 修复
- 修复「点击插入，无反应」：「插入编辑器」点击处理器整体加 try/catch 兜底，任何情况都会给出明确可见反馈
  （暂无内容 / 未找到正文编辑器 / 插入异常 均会弹出提示与原因），不再因内部未捕获异常而静默无反应。
- 用真实 Chrome 复测确认 PDF 转换链路正常（pdf.js 走 disableWorker 主线程，无需额外 worker 文件），
  控制台仅有一处无害的 workerSrc 弃用提示。

## [v2.0.20] - 2026-09-01
### 新增
- 「拖入」新增 PDF 直接转为公众号文章：引入 pdf.js 库（`stable/pdf.min.js`），支持拖入 `.pdf` 文件。
  - **文字版 PDF**（文字层可读取）：按 y 聚类成行、x 排序逐字重建，保留分页间隔，按字号自动识别标题层级（h1/h2/p），生成文字可复制、可套主题的真实文本段落。
  - **扫描版 / 纯图 PDF**（文字层近乎为空）：自动降级为逐页渲染高清图（宽最大 1100px、JPEG 0.82），保证扫描件也能直接拖入转成图文。
- 内容面板拖入提示、文件选择 accept、本地演示页（index.html）同步支持 `.pdf`；清单 `content_scripts` 与 `web_accessible_resources` 均已注册 `pdf.min.js`。

## [v2.0.19] - 2026-08-29
### 修复
- 修复「标题自动填充仍无效」的**真实根因**：公众号新版编辑器（ProseMirror）的标题框即使显示为空，
  其内部也含有零宽字符（\u200b）或内嵌换行。旧的判空逻辑只用 `.trim()` 去普通空白，会把
  「空的可见标题框」误判成「已填过」而整体跳过，导致 `findProseMirrorTitle()` 返回 null、
  又回退去填同名的**隐藏** `textarea#title`，可见标题框始终不同步（v2.0.18 只修了注入方式，
  没修判空，故仍无效）。
- 判空逻辑现先剔除所有零宽/不可见字符（\u200b-\u200f、\u2060-\u2069、\ufeff、\u00ad、\u061c、\u180e）
  与全部空白后再判断。已在真实公众号后台复刻 `findProseMirrorTitle()` 验证：修复后能正确命中可见
  标题框，且 `focus + selectAllChildren + execCommand('insertText')` 注入后标题实时可见。

## [v2.0.18] - 2026-08-29

### 修复

* 彻底修复「插入后标题同步到公众号标题框」仍失败的问题：公众号新版编辑器（ProseMirror）的文章标题框
  是一个**可见的** `.ProseMirror` 可编辑 div（外层容器 class 含 `title-editor__input`，本身没有
  placeholder 属性）。旧逻辑只能匹配到同名的**隐藏** `textarea#title`，把标题填进了隐藏字段，
  可见标题框从未更新，导致「标题一直同步不上」。

* 新增 `findProseMirrorTitle()`：优先定位可见标题框（contenteditable + 为空 + 位于页面上部 +
  宽 >200 / 高 15\~80px + 祖先容器 class 含 title 强加分），并能自动跳过正文/作者等其它编辑器。

* 对可见标题框采用 `focus + selectAllChildren + execCommand('insertText')` 注入——这是 ProseMirror
  认账、能让界面立即更新的唯一方式；同时回写隐藏的 `textarea#title` 供后端读取。已在真实公众号后台验证。

## \[v2.0.17] - 2026-08-28

### 修复

* 修复 v2.0.15 冒号对齐回归：列表项 <li> 中的「**条目**：值」不再把冒号掉到下一行（根因：之前只修了 <p>，没把 <li> 纳入键值对排版范围）。

* 修复「插入后标题同步到公众号标题框」失败的问题。现在 fillTitle 兼容多个公众号后台版本：
  · 12 个标准选择器 + 非空元素跳过（避免选到作者框或已填过的原标题框）。
  · 启发式兜底打分：id 含 \`title/文章标题、页面顶栏位置、空值、placeholder 强命中优先；
  含「作者/author」的 input/id/class 强扣分，避免把作者输入框错当标题框。
  · React/Vue 框架赋值走原生 setter + input/change/blur/propertychange 四类事件，避免被框架 diff 刷回空值。
  · 同步失败会最多重试 2 秒（4 次间隔 500ms），兼容标题框异步挂载的后台版本。

## \[v2.0.16] - 2026-08-28

### 新增

* 点击「插入编辑器」后，自动将文章标题同步到公众号文章的标题框。

* 标题来源优先级：面板输入框已有值 > 面板文档名（#jz-title）> 插入内容中第一个 h1。

* 仅在标题框为空时才填充，绝不覆盖用户手写的标题。

## \[v2.0.15] - 2026-08-28

### 修复

* md 键值对排版：标签改为左对齐、冒号与小标题保持同一行、超长值（如「原文链接」的
  URL）自动换行到下一行，不再被长链接挤坏。

* 移除误入 `stable/` 的 `_md-alignment-test.html`（下划线文件名触发扩展保留名校验，
  导致「无法加载扩展」）。

* 规范化发布目录：仓库以 `stable/` 为唯一稳定版，清理根目录旧 v2.0.0 冗余文件。

## \[v2.0.14] - 2026-08-28

### 修复

* .md 拖入预览软换行丢失（开启 markdown-it `breaks`），四行元数据被合并成一段的问题。

* 键值对冒号对齐逻辑调整。

## \[v2.0.13] - 2026-08-26

### 新增

* 标准 OOXML .docx 导出（Word / WPS 均可打开，图片以内联 `word/media` 部件嵌入）。

### 修复

* 图片按真实宽高等比缩放，宽度≤6 英寸、高度≤8 英寸，照片不再溢出 A4 页面。

## \[更早版本]

* 更早的版本变更请查阅 git 历史（`git log`）与 GitHub 上的 tag：`v-docx-2.0.13`、`v-html-pdf` 等。

