# 更新日志 (Changelog)

本文件记录「瑾之笺」各版本的功能变更。
版本留档规则：每个稳定版都会归档到 `releases/<版本>/`（完整可加载目录），
同时打 git tag `v<版本>`。最新版始终位于 `stable/`，旧版本不会覆盖、可随时回退使用。

## [v2.0.15] - 2026-08-28
### 修复
- md 键值对排版：标签改为左对齐、冒号与小标题保持同一行、超长值（如「原文链接」的
  URL）自动换行到下一行，不再被长链接挤坏。
- 移除误入 `stable/` 的 `_md-alignment-test.html`（下划线文件名触发扩展保留名校验，
  导致「无法加载扩展」）。
- 规范化发布目录：仓库以 `stable/` 为唯一稳定版，清理根目录旧 v2.0.0 冗余文件。

## [v2.0.14] - 2026-08-28
### 修复
- .md 拖入预览软换行丢失（开启 markdown-it `breaks`），四行元数据被合并成一段的问题。
- 键值对冒号对齐逻辑调整。

## [v2.0.13] - 2026-08-26
### 新增
- 标准 OOXML .docx 导出（Word / WPS 均可打开，图片以内联 `word/media` 部件嵌入）。
### 修复
- 图片按真实宽高等比缩放，宽度≤6 英寸、高度≤8 英寸，照片不再溢出 A4 页面。

## [更早版本]
- 更早的版本变更请查阅 git 历史（`git log`）与 GitHub 上的 tag：`v-docx-2.0.13`、`v-html-pdf` 等。
